<div class="tiny-elements">
    <h3>{{heading}}</h3>

    <form method="post" action="{{submiturl}}">
        <div class="actions mb-3">
            <div class="mb-2">
                <!-- "Back to Overview" -> acts as the Save button -->
                <button type="submit" class="btn btn-secondary">
                    {{navigateback}}
                </button>
            </div>
            <div>
                <a href="{{createelementurl}}" class="btn btn-primary">
                    {{createbuttonlabel}}
                </a>
            </div>
        </div>

        <table class="table generaltable">
            <thead>
                <tr>
                    <th>
                        <input type="checkbox" id="select-all">
                    </th>
                    <th>{{#str}} name, tiny_styles {{/str}}</th>
                    <th>{{#str}} type, tiny_styles {{/str}}</th>
                    <th>{{#str}} bootstrapclass, tiny_styles {{/str}}</th>
                    <th>{{#str}} actions, tiny_styles {{/str}}</th>
                </tr>
            </thead>
            <tbody>
                {{#elements}}
                <tr>
                    <td>
                        <input type="checkbox" name="selected_elements[]" value="{{id}}">
                    </td>
                    <td>{{name}}</td>
                    <td>{{type}}</td>
                    <td>{{bootstrapclass}}</td>
                    <td>
                            {{{toggleiconhtml}}}
                            {{{viewdetailsiconhtml}}}
                            {{{moveupiconhtml}}}
                            {{{movedowniconhtml}}}

                        <a href="{{editurl}}" title="{{#str}} editelement, tiny_styles {{/str}}" style="text-decoration: none;">
                            <i class="icon fa fa-cog fa-fw"></i>
                        </a>

                            {{{deleteiconhtml}}}
                    </td>
                </tr>
                {{/elements}}
            </tbody>
        </table>
    </form>
</div>
